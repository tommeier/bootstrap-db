<%= form_for(@time_datum) do |f| %>
  <% if @time_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_datum.errors.count, "error") %> prohibited this time_datum from being saved:</h2>

      <ul>
      <% @time_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_area :subject %>
  </div>
  <div class="field">
    <%= f.label :time_value %><br>
    <%= f.time_select :time_value %>
  </div>
  <div class="field">
    <%= f.label :timestamp_value %><br>
    <%= f.datetime_select :timestamp_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
